<h1>Create new question for the <%= @test.title %> test:</h1>

<%= form_with model: [@test, @question] do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_field :body %>
  </p>
  <p><%= form.submit %></p>
<% end %>

